name: Simple Test Workflow

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo hello
        run: echo "Hello, World!"

      - name: Show date
        run: date
